# this is some comments
extern putchard(ch);
extern printd(d);
extern scand();
fn foo(){
    for i=0;i<10{
        ;
    }
    return 0;
}
fn bar(a){
    if a<3{
        a=a+3;
    }else{
        a=a;
    }
    return a;
}
