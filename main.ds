# this is some comments
extern putchard(ch)
extern printd(d)
extern scand()
fn foo(){
    for i=0;i<10{
        if i<5{
            putchard(120+i)
        }else{
            printd(i)
        }
    }
}
fn fib(v){
    if v<3{
        1
    }else{
        fib(v-1)+fib(v-2)
    }
}
fn bar(val){
    var a=fib(10);
    var b=fib(15);
    if a+val<100{
        foo()
    }else{
        val/2
    }
}
fn calcFib(){
    var val=scand();
    fib(val)
}

